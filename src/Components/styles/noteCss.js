const css = (theme) => ({
  note: {
    padding: 25,
    margin: 10,
    maxWidth: 350,
    minWidth: 350,
    height: 300,
    cursor: "pointer",
    display: "flex",
    flexDirection: "column",
  },
  actionBtns: {
    display: "flex",
    justifyContent: "space-around",
    maxWidth: 200,
    margin: "auto auto 0px auto",
    alignSelf: "bottom",
  },
});

export default css;
